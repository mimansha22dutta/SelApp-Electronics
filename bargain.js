// Function to handle bargain offers (Could be further enhanced to handle server-side logic)
function handleBargain(productName, offerPrice) {
    console.log(`User wants to pay $${offerPrice} for ${productName}.`);
    // Implement logic to handle bargain offers (like sending them to the backend or offering a discount)
  }
  